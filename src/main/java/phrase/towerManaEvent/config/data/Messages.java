package phrase.towerManaEvent.config.data;

import java.util.List;

public record Messages(String noPermission, String unknownCommand, String error, String incorrectArguments,
                       String notAPlayer) {
}
